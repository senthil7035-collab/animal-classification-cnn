# animal-classification-cnn
🐾 Animal Classification using CNN — A deep learning project that uses a Convolutional Neural Network (CNN) to classify animal images into different categories. The model is trained on an animal image dataset and predicts the animal class with a confidence score. Built using Python, TensorFlow/Keras, and computer vision techniques.
